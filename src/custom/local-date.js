/**
 * @type {{
 *   locales: string,
 *   options: {
 *     day: "numeric" | "2-digit",
 *     month: "numeric" | "2-digit" | "long" | "short" | "narrow",
 *     year: "numeric" | "2-digit"
 *   }
 * }}
 */
export const localeDateString = {
    locales: "en-US",
    options: {
        day: "numeric",
        month: "long",
        year: "numeric",
    },
};
